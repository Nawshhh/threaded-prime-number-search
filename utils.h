#ifndef UTILS_H
#define UTILS_H

#include <string>

/**
 * @brief Gets the current time as a formatted string.
 * @return A string representing the current timestamp (YYYY-MM-DD HH:MM:SS).
 */
std::string getCurrentTimestamp();

#endif // UTILS_H